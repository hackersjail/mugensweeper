tags:
  - name: Ryoko
    description: Ryoko API
/example:
  get:
    tags:
      - Ryoko
    produces:
      - "text/html; charset=utf-8"
    parameters:
      - name: "string"
        in: "query"
        description: "Example query parameter"
        required: true
        type: "string"
    responses:
      200:
        description: "success"
        schema:
          type: "string"
          example: "text"
