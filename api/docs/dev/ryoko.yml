tags:
  - name: Ryoko field
    description: Ryoko field API
/ryoko/field:
  get:
    tags:
      - Ryoko field
    produces:
      - "text/html; charset=utf-8"
    parameters:
      - name: "body"
        in: "body[0]"
        description: "Ryoko body parameter"
        required: true
        type: "string"
    responses:
      200:
        description: "success"
        schema:
          type: "string"
          example: "text"
/ryoko/block/:
  post:
    tags:
      - Ryoko block
    produces:
      - "text/html; charset=utf-8"
    parameters:
      - name: "x"
        in: "query"
        description: "Ryoko body parameter"
        required: true
        type: "string"
      - name: "y"
        in: "query"
        description: "Ryoko query parameter"
        required: true
        type: "string"
    responses:
      200:
        description: "success"
        schema:
          type: "string"
          example: "text"
