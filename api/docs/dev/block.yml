tags:
  - name: Ryoko block
    description: Ryoko block API
/ryoko/block/:
  post:
    tags:
      - Ryoko block
    produces:
      - "text/html; charset=utf-8"
    parameters:
      - name: "x"
        in: "query"
        description: "Ryoko body parameter"
        required: true
        type: "string"
      - name: "y"
        in: "query"
        description: "Ryoko query parameter"
        required: true
        type: "string"
    responses:
      200:
        description: "success"
        schema:
          type: "string"
          example: "text"
